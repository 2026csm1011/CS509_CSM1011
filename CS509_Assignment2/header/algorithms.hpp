#ifndef ALGORITHMS_HPP
#define ALGORITHMS_HPP
#include "csr.hpp"
#include <vector>
#include <string>

// Runners that handle parsing, executing, and timing
void run_bellman_ford_task(std::string filename);
void run_floyd_warshall_task(std::string filename);
#endif